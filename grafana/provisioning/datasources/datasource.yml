apiVersion: 1

datasources:
  - name: VULNSIGHT PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: vulnsight
    database: vulnsight
    isDefault: true
    editable: true
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    secureJsonData:
      password: vulnsight
